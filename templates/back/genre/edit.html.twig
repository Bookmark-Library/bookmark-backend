{% extends 'base.html.twig' %}

{% block title %}Modifier le genre
{% endblock %}

{% block body %}
	<h1>Modifier le genre
	</h1>

	{{ include('back/genre/_form.html.twig', {'button_label': 'Modifier'}) }}

	<a href="{{ path('app_back_genre_index') }}" class="btn btn-primary">Retour à la liste</a>

	{{ include('back/genre/_delete_form.html.twig') }}
{% endblock %}
