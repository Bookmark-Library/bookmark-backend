<form method="post" action="{{ path('app_back_book_delete', {'id': book.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet élément ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book.id) }}">
	<button class="btn btn-danger mt-1">Supprimer</button>
</form>
