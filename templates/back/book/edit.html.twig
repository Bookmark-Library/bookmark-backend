{% extends 'base.html.twig' %}

{% block title %}Modifier le livre : "{{ book.title }}"
{% endblock %}

{% block body %}
	<h1>Modifier le livre : "{{ book.title }}"</h1>

	{{ include('back/book/_form.html.twig', {'button_label': 'Modifier'}) }}

	<a href="{{ path('app_back_book_index') }}" class="btn btn-primary">Retour à la liste</a>

	{{ include('back/book/_delete_form.html.twig') }}
{% endblock %}
