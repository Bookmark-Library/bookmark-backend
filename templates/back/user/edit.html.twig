{% extends 'base.html.twig' %}

{% block title %}Modifier l'utilisateur : "{{ user.alias }}"
{% endblock %}

{% block body %}
	<h1>Modifier l'utilisateur : "{{ user.alias }}"</h1>

	{{ include('back/user/_form.html.twig', {'button_label': 'Modifier'}) }}

	<a href="{{ path('app_back_user_index') }}" class="btn btn-primary">Retour à la liste</a>

	{{ include('back/user/_delete_form.html.twig') }}
{% endblock %}
