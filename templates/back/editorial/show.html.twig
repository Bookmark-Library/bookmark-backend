{% extends 'base.html.twig' %}

{% block title %}Editorial{% endblock %}

{% block body %}
    <h1>Editorial</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ editorial.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ editorial.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ editorial.content }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ editorial.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_editorial_index') }}">back to list</a>

    <a href="{{ path('app_back_editorial_edit', {'id': editorial.id}) }}">edit</a>

    {{ include('back/editorial/_delete_form.html.twig') }}
{% endblock %}
